# Chatterbox TTS Audiobook Edition - Mac Requirements
# Use this file instead of requirements.txt on macOS
# PyTorch is intentionally excluded here — install it first via conda:
#   conda install pytorch::pytorch torchvision torchaudio -c pytorch

# Core TTS dependencies
numpy==1.26.0
resampy==0.4.3
librosa==0.10.0
s3tokenizer
transformers==4.46.3
diffusers==0.29.0
resemble-perth==1.0.1
omegaconf==2.3.0
conformer==0.3.2
spacy>=3.4.0

# Compatibility
pydantic==2.10.6

# Web interface
gradio
